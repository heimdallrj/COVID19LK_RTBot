
const config = {
    consumer_key: "_",
    consumer_secret: "_",
    access_token: "_",
    access_token_secret: "_"
};

export const interval = 3600 * 1000; // 1 hour
export const searchQuery = { q: "#HASH_TAG", count: 1, result_type: "recent" };

export default config;
